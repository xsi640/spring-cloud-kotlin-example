server:
  port: 8020
spring:
  application:
    name: api-gateway
app:
  id: ${spring.application.name}
apollo:
  meta: http://192.168.61.100:8071
  #cluster:
  #cacheDir
  bootstrap:
    enabled: true
    namespaces: application,dev.cloud,dev.redis
env: DEV