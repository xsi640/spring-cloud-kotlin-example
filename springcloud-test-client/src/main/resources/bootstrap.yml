server:
  port: 8082
spring:
  application:
    name: test-client
app:
  id: ${spring.application.name}
apollo:
  meta: http://192.168.61.100:8071
  #cluster:
  #cacheDir
  bootstrap:
    enabled: true
    namespaces: application,dev.cloud
env: DEV