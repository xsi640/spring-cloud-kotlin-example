server:
  port: 8081
spring:
  application:
    name: test-server
app:
  id: ${spring.application.name}
apollo:
  meta: http://192.168.61.100:8071
  # cluster:
  # cacheDir:
  bootstrap:
    enabled: true
    namespaces: application,dev.cloud,dev.mysql
env: DEV